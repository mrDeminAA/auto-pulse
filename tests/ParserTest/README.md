# Тестирование парсеров AutoPulse

## Быстрый старт

### 1. Запуск теста Che168 (Китай)

```bash
cd tests/ParserTest
dotnet run
```

### 2. Запуск теста Mobile.de (Европа)

Откройте `Program.cs` и замените:
```csharp
await TestChe168Async(browser, loggerFactory);
```
на:
```csharp
await TestMobileDeAsync(browser, loggerFactory);
```

### 3. Запуск теста Cars.com (США)

Откройте `Program.cs` и замените:
```csharp
await TestChe168Async(browser, loggerFactory);
```
на:
```csharp
await TestCarsComAsync(browser, loggerFactory);
```

---

## Требования

1. **Playwright браузеры**:
```bash
dotnet tool install --global Microsoft.Playwright.CLI
dotnet playwright install chromium
```

2. **Интернет** — парсеры работают через HTTP запросы

3. **Отсутствие блокировок** — некоторые сайты могут блокировать запросы из РФ

---

## Возможные проблемы

### 1. Таймаут ожидания элементов
```
warn: Таймаут ожидания элементов автомобилей
```
**Решение:** Сайт мог изменить структуру. Нужно обновить CSS селекторы в парсере.

### 2. 0 найденных автомобилей
```
✅ Найдено автомобилей: 0
```
**Причины:**
- Блокировка по IP (нужен прокси)
- Изменилась структура сайта
- Капча/бот-защита

### 3. Ошибка Playwright
```
Executable doesn't exist
```
**Решение:**
```bash
dotnet playwright install chromium
```

---

## Конвертация валют

Парсеры возвращают цены в локальных валютах:
- 🇨🇳 Che168 → CNY (юани)
- 🇪🇺 Mobile.de → EUR (евро)
- 🇺🇸 Cars.com → USD (доллары)

Для конвертации в RUB используйте примерные курсы:
- 1 CNY ≈ 13.5 RUB
- 1 EUR ≈ 100 RUB
- 1 USD ≈ 90 RUB

---

## Интеграция с Worker

Worker автоматически использует все парсеры при наличии регионов в запросе:

```json
{
  "brandId": 1,
  "modelId": 1,
  "yearFrom": 2015,
  "yearTo": 2020,
  "regions": "[\"china\",\"europe\",\"usa\"]"
}
```

Worker выполнит:
1. `ParseChinaAsync()` → Che168PlaywrightParser
2. `ParseEuropeAsync()` → MobileDeParser
3. `ParseUsaAsync()` → CarsComParser

---

**Последнее обновление:** 20 Февраля 2026
